# Copy this file to .env before running docker-compose:
#   cp .env.example .env
#
# Test credentials are provided below — you can use them as-is without
# creating your own API keys. They are shared intentionally for evaluation.

# OpenWeatherMap API key (shared for evaluation)
OPENWEATHER_API_KEY=5368f806c5b7ec1c3033f51b8127e72f

# MongoDB Atlas (shared for evaluation)
MONGODB_URL=mongodb+srv://jinbinng0706:jinabcd1234@weather-app.rpgbnbd.mongodb.net/?appName=Weather-app
MONGODB_DB=weather-app

# Comma-separated list of origins allowed to call the backend
ALLOWED_ORIGINS=http://localhost:3000,http://frontend:3000

# Optional (docker-compose sets this automatically via service name)
# BACKEND_URL=http://localhost:8000
